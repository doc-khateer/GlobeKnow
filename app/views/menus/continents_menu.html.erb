<h1>What Do You GlobeKnow About...</h1>

<table>
<% continents.each do |continent| %>
	<%= link_to image_tag(continent.photo.url), continent_path(continent), style:"continentbox" %>
	...<%= continent.name %>?
<% end %>
